pipeline{
    agent any
    stages{
        stage('Build Stage') {
            steps{
            echo "First build"
        }
        }
        stage('Testing Stage'){
            steps{
                echo "Testing the build"
        }
        }
        stage('Deployment Stage'){
            steps{
                echo "Deploy the build"
        }
        }
    }
}
